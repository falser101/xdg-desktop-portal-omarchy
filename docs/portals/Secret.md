# Secret

状态：**委托** → `gnome-keyring`  
对照：KDE 侧通常走 KWallet / secrets

## 已完成

- `org.freedesktop.impl.portal.Secret` → `org.freedesktop.secrets`（gnome-keyring）

## 延后

- 不换钱包后端，除非 Omarchy 统一改用其它 secrets 服务
